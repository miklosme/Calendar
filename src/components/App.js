import React from 'react';
import AddEvent from '../containers/AddEvent';
import Events from '../containers/Events';

const App = () => (
  <div>
    <AddEvent />
    <Events />
  </div>
);

export default App;
